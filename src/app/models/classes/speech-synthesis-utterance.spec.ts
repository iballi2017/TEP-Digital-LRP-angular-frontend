import { SpeechSynthesisUtterance } from './speech-synthesis-utterance';

describe('SpeechSynthesisUtterance', () => {
  it('should create an instance', () => {
    expect(new SpeechSynthesisUtterance()).toBeTruthy();
  });
});
